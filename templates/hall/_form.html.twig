<div class="container">

	<div class="row">
		<div class="col-md-8">
			{{ form_start(form) }}
			{{ form_widget(form) }}
			<div class="d-flex gap-3 mt-3">
				<button class="btn btn-success">{{ button_label|default('Save') }}</button>
				<a href="{{ path('app_hall_index') }}" class="btn btn-secondary">To Hall List</a>
			</div>
			{{ form_end(form) }}
		</div>
	</div>


</div>
